import 'package:flutter/cupertino.dart';

class WaifuScreen extends StatelessWidget{
  const WaifuScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child:  Center(
        child: Text('Waifu Screen'),
      ),
      alignment: Alignment.center,
    );
  }

}